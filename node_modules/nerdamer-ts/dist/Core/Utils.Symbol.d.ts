import { Symbol } from '../Types/Symbol';
/**
 * Returns the sum of an array
 * @param {Array} arr
 * @param {boolean} toNumber
 * @returns {Symbol}
 */
export declare function arraySum(arr: any[], toNumber?: boolean): number | Symbol;
