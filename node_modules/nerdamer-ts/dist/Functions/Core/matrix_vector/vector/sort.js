"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.sort = void 0;
const Vector_1 = require("../../../../Types/Vector");
const Utils_1 = require("../../../../Core/Utils");
function sort(symbol, opt) {
    opt = opt ? opt.toString() : 'asc';
    var getval = function (e) {
        if (e.group === N)
            return e.multiplier;
        if (e.group === FN) {
            if (e.fname === '')
                return getval(e.args[0]);
            return e.fname;
        }
        if (e.group === S)
            return e.power;
        return e.value;
    };
    var symbols = (0, Utils_1.isVector)(symbol) ? symbol.elements : symbol.collectSymbols();
    return new Vector_1.Vector(symbols.sort(function (a, b) {
        var aval = getval(a), bval = getval(b);
        if (opt === 'desc')
            return bval - aval;
        return aval - bval;
    }));
}
exports.sort = sort;
//# sourceMappingURL=sort.js.map