/**
 * Returns the rectangular form of a complex number. Does not work for symbolic coefficients
 * @param {Symbol} symbol
 * @returns {Symbol}
 */
export function rectform(symbol: Symbol): Symbol;
import { Symbol } from "../../../Types/Symbol";
