/**
 * The mod function
 * @param {Symbol} symbol1
 * @param {Symbol} symbol2
 * @returns {Symbol}
 */
export function mod(symbol1: Symbol, symbol2: Symbol): Symbol;
import { Symbol } from "../../../Types/Symbol";
