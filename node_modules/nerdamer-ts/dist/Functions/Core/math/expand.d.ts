/**
 * Expands a symbol
 * @param symbol
 */
export function expand(symbol: any, opt?: undefined): any;
/**
 * A wrapper for the expand function
 * @param {Symbol} symbol
 * @returns {Symbol}
 */
export function expandall(symbol: Symbol, opt: any): Symbol;
import { Symbol } from "../../../Types/Symbol";
