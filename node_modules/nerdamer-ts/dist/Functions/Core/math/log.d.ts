/**
 * The log function
 * @param {Symbol} symbol
 * @param {Symbol} base
 * @returns {Symbol}
 */
export function log(symbol: Symbol, base: Symbol, ...args: any[]): Symbol;
import { Symbol } from "../../../Types/Symbol";
