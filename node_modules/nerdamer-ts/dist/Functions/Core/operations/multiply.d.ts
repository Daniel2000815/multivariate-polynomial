/**
 * Gets called when the parser finds the * operator. See this.add
 * @param {Symbol} a
 * @param {Symbol} b
 * @returns {Symbol}
 */
export function multiply(a: Symbol, b: Symbol): Symbol;
import { Symbol } from "../../../Types/Symbol";
